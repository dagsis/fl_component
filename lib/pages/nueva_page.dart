import 'package:flutter/material.dart';

class NuevaPage extends StatelessWidget {
  const NuevaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: Center(
        child: Text('Nueva Pagina'),
      ),
    );
  }
}
